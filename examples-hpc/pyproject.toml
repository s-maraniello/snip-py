[project]
name = "snip-py-hpc"
version = "0.1.0"
description = "High Performance Computing examples in Python"
requires-python = ">=3.13"
dependencies = [
    "ipython",
    "line_profiler",
    "snakeviz",
    "memory_profiler",
    "psutil",
]
